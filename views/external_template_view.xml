<odoo>
    <template id="external_layout_standard_sangaspar" inherit_id="web.external_layout_standard" >
        <!-- support for custom header -->
        <div t-attf-class="header o_company_#{company.id}_layout" position="replace">
            <t t-set="report_header_style">padding-top:6.2mm; padding-left:1mm; padding-right:1mm;height:30px</t>
            <!--t-att-style="report_header_style"-->
            <div t-attf-class="header o_company_#{company.id}_layout" t-att-style="report_header_style">
                <div class="col-12">
                    <h2  class="text-center">
                        <img t-if="company.logo" t-att-src="image_data_uri(company.logo)" style="max-height: 70px;" alt="Logo"/>
                    </h2>
                     <div class="row col-12 mb32 text-center">
                        <div class="col-12" style="margin-top:1px;font-size:14px;line-height: normal;" t-field="company.report_header" name="moto"/>
                    </div>
                </div>

                <div t-if="company.logo or company.report_header" class="row zero_min_height">
                    <div class="col-12">
                        <div style="border-bottom: 1px solid black;"/>
                    </div>
                </div>
                <div class="row">
                    <div class="col-12" name="company_address">
                        <span t-if="company.company_details" style="font-size:14px;" t-field="company.company_details"></span>
                    </div>
                </div>

            </div>
        </div>

    </template>
</odoo>